import "math"

log(sin(65))

const aspect = input('aspect ratio: ')
const width = input('width: ') / 25.6

const height = width / aspect;


const diagonal = (width**2 + height ** 2) ** (1/2)
log(diagonal, 'inches')