completed:
    - while loops
    - subs (functions)
    - add return to functions
    - add break
    - add continue
    - allow functions parameters to be passed in
    - create sub return values
    - make local variables that delete after the function finishes
    - add math module
    - import
    - selective importing
    - fix close square bracket not being allowed in open indentation
    - items (arrays)
    - add else if to if statments
    - add try catch

incompleted:
    - books (objects)
    - add del to delete variables
    - add origin to translate.go
    - add edit variable assignment
    - allow negetive variables without needing 0 in front (e.g. -x instead of 0-x)
    - add index and key to variable assignment and reading
    - fix atan function in maths module
